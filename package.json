{
  "name": "@centrapi/hub",
  "version": "1.0.0",
  "description": "a simple service registry and client for registering and connecting to other services",
  "main": "index.js",
  "scripts": {
    "compile": "npx lerna run compile",
    "bootstrap": "npx lerna bootstrap",
    "postinstall": "npm run bootstrap && npm run compile"
  },
  "author": "Micah Riggan",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.13.4",
    "typescript": "^3.4.5"
  }
}
